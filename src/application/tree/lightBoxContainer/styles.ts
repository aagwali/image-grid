import styled from "styled-components"

import { HStack, Switch, theme } from "@chakra-ui/react"

export const SwitchBox = styled(HStack)`
  margin-right: ${theme.space[2.5]};
`

export const QualitySwitch = styled(Switch)``
